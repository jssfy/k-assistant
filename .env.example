# ==================================================
# FastAPI 后端 (app/config.py → pydantic-settings)
# ==================================================

# 数据库连接
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/k_assistant

# 后端连接 LiteLLM 代理的地址和密钥（需与 litellm_config.yaml 的 master_key 一致）
LITELLM_BASE_URL=http://localhost:4000/v1
LITELLM_API_KEY=sk-1234

# 默认使用的模型名（对应 litellm_config.yaml 中的 model_name）
DEFAULT_MODEL=claude-sonnet

# 运行环境：development 用 ConsoleRenderer 日志，production 用 JSON 日志
APP_ENV=development

# ==================================================
# Docker Compose → LiteLLM 容器 (docker-compose.yml)
# ==================================================

# Anthropic API 密钥（必填，LiteLLM 用此调用 Anthropic）
ANTHROPIC_API_KEY=sk-ant-xxx

# Anthropic API 代理地址（可选，不填则直连 api.anthropic.com）
# ANTHROPIC_BASE_URL=https://api.minimaxi.com/anthropic

# OpenAI API 密钥（可选，如需 GPT 模型）
# OPENAI_API_KEY=sk-xxx
